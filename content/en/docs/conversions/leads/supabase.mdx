---
title: "Supabase"
description: "Learn how to track lead conversion events with Supabase and RevRoute"
---

# Supabase

import ConversionTrackingPrerequisites from '../../../../../snippets/en/conversion-tracking-prerequisites.mdx'
import LeadAttributes from '../../../../../snippets/en/lead-attributes.mdx'
import LeadsIntro from '../../../../../snippets/en/leads-intro.mdx'
import LeadsOutro from '../../../../../snippets/en/leads-outro.mdx'

<LeadsIntro />

In this guide, we will be focusing on tracking new user sign-ups for a SaaS application that uses Supabase for user authentication.

<ConversionTrackingPrerequisites />

## Configure Supabase

Next, configure Supabase to track lead conversion events in the auth callback function.

Here's how it works in a nutshell:

1. In the `/api/auth/callback` route, check if:
   - the `rr_id` cookie is present.
   - the user is a new sign up (created in the last 10 minutes).
2. If the `rr_id` cookie is present and the user is a new sign up, send a lead event to RevRoute using `revroute.track.lead`
3. Delete the `rr_id` cookie.

<CodeGroup>

```typescript Next.js App Router
// app/api/auth/callback/route.ts
import { cookies } from "next/headers";
import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { waitUntil } from "@vercel/functions";
import { revroute } from "@/lib/revroute";

export async function GET(request: Request) {
  const { searchParams, origin } = new URL(request.url);
  const code = searchParams.get("code");
  // if "next" is in param, use it as the redirect URL
  const next = searchParams.get("next") ?? "/";

  if (code) {
    const supabase = createClient(cookies());
    const { data, error } = await supabase.auth.exchangeCodeForSession(code);
    if (!error) {
      const { user } = data;
      const rr_id = cookies().get("rr_id")?.value;
      // if the user is created in the last 10 minutes, consider them new
      const isNewUser =
        new Date(user.created_at) > new Date(Date.now() - 10 * 60 * 1000);
      // if the user is new and has a rr_id cookie, track the lead
      if (rr_id && isNewUser) {
        waitUntil(
          revroute.track.lead({
            clickId: rr_id,
            eventName: "Sign Up",
            customerExternalId: user.id,
            customerName: user.user_metadata.name,
            customerEmail: user.email,
            customerAvatar: user.user_metadata.avatar_url,
          }),
        );
        // delete the clickId cookie
        cookies().delete("rr_id");
      }
      return NextResponse.redirect(`${origin}${next}`);
    }
  }

  // return the user to an error page with instructions
  return NextResponse.redirect(`${origin}/auth/auth-code-error`);
}
```

```typescript Next.js Pages Router
// pages/api/auth/callback.ts
import { NextApiRequest, NextApiResponse } from "next";
import { createClient } from "@supabase/supabase-js";
import { revroute } from "@/lib/revroute";

export default async function handler(
  req: NextApiRequest,
  res: NextApiResponse,
) {
  const { code, next = "/" } = req.query;
  const origin = `${req.headers["x-forwarded-proto"]}://${req.headers.host}`;

  if (typeof code === "string") {
    const supabase = createClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.SUPABASE_SERVICE_ROLE_KEY!,
    );

    const { data, error } = await supabase.auth.exchangeCodeForSession(code);

    if (!error) {
      const { user } = data;
      const { rr_id } = req.cookies;

      // if the user is created in the last 10 minutes, consider them new
      const isNewUser =
        new Date(user.created_at) > new Date(Date.now() - 10 * 60 * 1000);

      // if the user is new and has a rr_id cookie, track the lead
      if (rr_id && isNewUser) {
        revroute.track
          .lead({
            clickId: rr_id,
            eventName: "Sign Up",
            customerExternalId: user.id,
            customerName: user.user_metadata.name,
            customerEmail: user.email,
            customerAvatar: user.user_metadata.avatar_url,
          })
          .catch(console.error); // Handle any errors in tracking

        // delete the clickId cookie
        res.setHeader(
          "Set-Cookie",
          `rr_id=; Path=/; Expires=Thu, 01 Jan 1970 00:00:00 GMT`,
        );
      }

      return res.redirect(`${origin}${next}`);
    }
  }

  // return the user to an error page with instructions
  return res.redirect(`${origin}/auth/auth-code-error`);
}
```

</CodeGroup>

<LeadAttributes />

<LeadsOutro />
