---
title: "Segment"
description: "Узнайте, как отслеживать события конверсий продаж с помощью Segment и Revroute"
---

<Tip>
  Эта функция доступна только на [тарифных планах Business и
  выше](https://revroute.ru/pricing).
</Tip>

[Интеграция Revroute с Segment](https://revroute.ru/integrations/segment) обеспечивает двунаправленную синхронизацию между вашим рабочим пространством Segment и Revroute. Интеграция состоит из 2 частей:

- [Revroute (Actions) Destination](https://www.twilio.com/docs/segment/connections/destinations/catalog/actions-dub) — отслеживание событий конверсий в Revroute непосредственно из Segment
- [Revroute Source](https://www.twilio.com/docs/segment/connections/sources/catalog/cloud-apps/dub) — отправка событий клиентов из Revroute в Segment

В этом руководстве мы сосредоточимся на использовании [Revroute (Actions) Destination](https://www.twilio.com/docs/segment/connections/destinations/catalog/actions-dub) для отслеживания событий конверсий непосредственно из Segment.

<ConversionTrackingPrerequisites />

## Настройка Segment Action

Далее настройте [Segment Revroute (Actions)](https://app.segment.com/goto-my-workspace/destinations/catalog/actions-dub) для отслеживания событий конверсий продаж.

<Steps>

  <Step title="Добавьте назначение Revroute (Actions)">

    Перейдите к [Segment Revroute (Actions)](https://app.segment.com/goto-my-workspace/destinations/catalog/actions-dub) и добавьте назначение в ваше рабочее пространство Segment.

    ![Segment Revroute (Actions) destination](/images/conversions/segment/segment-actions.png)

  </Step>

  <Step title="Настройте API-ключ Revroute">

    В настройках назначения Revroute (Actions) заполните следующие поля:

    - **Name:** введите имя для идентификации этого назначения в Segment.
    - **API Key:** введите ваш [API-ключ Revroute](/docs/api-reference/authentication#api-keys). Его можно найти в [панели управления Revroute](https://app.revroute.ru/settings/tokens).
    - **Enable Destination:** включите, чтобы разрешить Segment отправлять данные в Revroute.

    После заполнения нажмите **Save Changes**.

    ![Segment Revroute (Actions) Basic Settings](/images/conversions/segment/segment-basic-settings.png)

  </Step>

  <Step title="Добавьте маппинг">

    Далее, в зависимости от события, которое вы хотите отслеживать, выберите соответствующее действие из списка доступных:

    | Отслеживаемое событие | Имя события по умолчанию | Пример |
    | --- | --- | --- |
    | Лид | Sign Up | Пользователь регистрирует аккаунт |
    | Продажа | Order Completed | Пользователь покупает продукт |

    ![Segment Revroute (Actions) Mapping](/images/conversions/segment/segment-track-sale-action.png)

    Под выбранным действием вы увидите маппинг для этого действия.

    ![Segment Revroute (Actions) Mapping](/images/conversions/segment/segment-track-sale-mapping.png)

    Вы можете настроить триггер и маппинг в соответствии с конкретными потребностями вашего приложения.

    Наконец, нажмите **Next**, а затем **Save and enable**, чтобы добавить маппинг к назначению.

  </Step>

  <Step title="Отправьте события конверсий в Revroute">

    На серверной стороне используйте SDK `@segment/analytics-node` для отправки событий конверсий в Segment.

    Убедитесь, что в полезную нагрузку включены соответствующие свойства:

  <CodeGroup>

    ```tsx Track a lead
    import { Analytics } from "@segment/analytics-node";

    const segment = new Analytics({
      writeKey: "<YOUR_SEGMENT_WRITE_KEY>",
    });

    const cookieStore = await cookies();
    const clickId = cookieStore.get("dub_id")?.value;

    segment.track({
      userId: id,
      event: "Sign Up",
      context: {
        traits: {
          name,
          email,
          avatar,
          clickId,
        },
      },
      integrations: {
        All: true,
      },
    });
    ```

    ```tsx Track a sale
    import { Analytics } from "@segment/analytics-node";

    const segment = new Analytics({
      writeKey: "<YOUR_SEGMENT_WRITE_KEY>",
    });

    segment.track({
      userId: id,
      event: "Order Completed",
      properties: {
        payment_processor: "stripe",
        order_id: "ORD_123",
        currency: "USD",
        revenue: 1000,
      },
      integrations: {
        All: true,
      },
    });
    ```

  </CodeGroup>

    После отслеживания события Segment перенаправит данные конверсии в Revroute на основе настроенных маппингов.

  </Step>

</Steps>

## Поддерживаемые атрибуты

<Tabs>
  <Tab title="Событие лида">

    <LeadAttributes />

  </Tab>
  <Tab title="Событие продажи">

    <SaleAttributes />

  </Tab>
</Tabs>

## Просмотр результатов конверсий

Вот и всё — настройка завершена! Теперь вы можете наблюдать за ростом дохода от конверсий. Мы предоставляем 3 различных представления для анализа ваших конверсий:

<ViewConversions />

## Примеры приложений

<Card
  title="Пример Segment + Next.js App Router"
  icon="github"
  href="https://github.com/dubinc/examples/blob/main/conversions/segment/actions/track-sale.ts"
>
  Приложение Next.js, использующее Segment для отслеживания продаж.
</Card>
